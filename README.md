# loopstation (wip)
CLI recorder for lazy tests

`lpst.py record [argv]`: record your already-working CLI working

`lpst.py playback [directory]`: playback previously working tests to make sure they still work
